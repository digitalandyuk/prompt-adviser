interface:
  display_name: "Prompt Adviser V2"
  short_description: "Score and refine prompts before execution"
  default_prompt: "Use $prompt-adviser-v2 to review my ChatGPT, OpenAI or Codex draft. Ask three score-led questions plus 'Shall I ask more?' and put the copyable prompt last."

policy:
  allow_implicit_invocation: true
